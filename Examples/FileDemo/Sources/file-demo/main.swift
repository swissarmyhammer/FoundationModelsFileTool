// `file-demo` — example executable exercising the file operations through the
// `OperationsCLI` command-tree driver (and, in `--chat`/`--script` modes, a
// `LanguageModelSession`).
//
// Scaffold placeholder: the real command wiring — assembling the five file
// operations into an ArgumentParser command tree, the scripted `--chat` harness
// that drives the read → anchor-edit → diagnostics-clean loop, and the
// `--script` sequential mode — lands in the FileDemo task. For now this keeps
// the executable target compiling.
print("file-demo: scaffold — command wiring lands in the FileDemo task")
