// `FileTool` — the FoundationModels file tool library.
//
// This file is the scaffold placeholder that establishes the `FileTool` module
// so the package builds and the test targets can `@testable import` it. The
// real contents — the five `@Generable @Operation` structs (`read file`,
// `write file`, `edit file`, `glob files`, `grep files`), `FileContext`,
// `PathGuard`, `Hashline`, `EditEngine`, `AtomicWriter`, the glob/grep engines,
// the `DiagnosticsBridge`, and the typed outputs — land in the subsequent tasks
// and replace this comment.
